# URL base de la API backend.
# En local apunta al Express local; cuando el proyecto se despliegue
# (Parte 2) se cambia por la URL publica del backend, sin tocar codigo.
VITE_API_URL=http://localhost:4000/api
